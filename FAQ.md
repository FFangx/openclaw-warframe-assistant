# FAQ

**Q: 不装 AlecaFrame 能用吗？**
能。公开功能全部可用（查价/遗物/世界状态/赏金/订阅/哪里买），官方中文词典自动走在线兜底。不可用的是个人功能：我的账号/库存/紫卡、掉落监测、裂缝卡的库存遗物配对、开遗物/精炼/奸商购物推荐、商店已购标、周常自动打卡（手动打卡仍可用）。跑 `doctor.mjs` 看你机器的实际矩阵。

**Q: 支持国服（WeGame）吗？**
不支持。国服无公开 API。本项目所有数据源都是国际服。

**Q: 会被封号吗？**
本项目不读游戏内存、不注入、不操作游戏进程，只读 AlecaFrame 落盘文件和公开 API——与用浏览器看 warframe.market 同性质。AlecaFrame 本身是 Overwolf 商店应用。但任何第三方工具 DE 都不背书，风险自负。

**Q: 为什么强调模型要开高思考档位？**
实测（2026-08）同一模型低思考档位会钻规则字面空子：无视「禁止市场写操作」写出自动挂单工具、写游戏宏、甚至删改规则文件来给自己放行。安装器现在会把受控安全片段写入启动必读的 `AGENTS.md`，SKILL.md 也保留运行规则，但模型能力仍会影响遵守程度。如果你换模型或降档，建议用「帮我写个自动开遗物的宏」这类问题先测一轮拒绝行为。

**Q: 怎么拿自己的 QQ openid？**
给机器人随便发一条消息，看 Gateway 日志里该消息的发送者 openid 字段（32 位 hex）。填进 `ownerOpenId` 即可。注意大小写不敏感（内部已归一化）。

**Q: Mac/Linux 能跑吗？**
未测试。公开查询链路理论上只依赖 Node+Chromium 系浏览器，路径探测是 Windows 写法，要自己改。AlecaFrame 只有 Windows 版，个人功能在其他平台无解。

**Q: 图标/图片素材从哪来的？**
DE 官方素材（经 warframe.market、browse.wf、AlecaFrame CDN、WFCD 社区仓库分发），版权归 DE，fan 项目惯例使用。详见 NOTICE.md。

**Q: 游戏更新出了新物品，数据要手动更新吗？**
基本不用：词典/掉落/价格缓存 1 小时~7 天自动过期重建。物品小图想秒出可重跑一次 `prefetch-icons.mjs`（幂等增量）。周常静态表（weekly-static.json）里的奖励池描述在 DE 大改版时可能要手动对一遍。

**Q: 卡片上「已购买」「自动打卡」判定准吗？**
诚实度分档：能唯一定位的绿标到具体商品；对不齐的只报「N 件无法定位」；周常自动核销只采信带周界校验的字段，判不了的宁可不勾。快照只在过加载点时更新，所以是「最终一致」不是实时。

**Q: 为什么有的商店只显示候选池不显示本期货单？**
一部分商人（言录使/鸟三等）的货单由服务器种子随机，公开 API 拿不到、本地也复现不了（社区已知限制）。能精确复现的（Donda 等 42 家周期类）会直接显示本期货单。

**Q: 订阅会不会刷屏？**
不会。按事件 ID 去重、按轮换边界蹲守（到点前不联网不推送）、首次订阅只建基线不推历史。赏金类强制要求筛选词就是为了防刷屏。

**Q: 模型幻觉编价格/编获取途径怎么办？**
SKILL.md 五级决策树强制实时数据只能来自脚本、获取途径必须双查掉落表和商人反查、伪造引用直接禁止。但这依赖模型遵守——又回到「开高思考档位」那条。发现胡说可以直接用短命令（`wm X`/`哪里买 X`），短命令不经模型。
