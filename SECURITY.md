# 安全策略（Security Policy）

本项目由个人维护者业余维护，**不承诺任何 SLA**（响应时间、修复时限均不保证，见 [SUPPORT.md](SUPPORT.md)）。
尽管如此，安全问题会得到优先处理。

## 支持的版本

只维护当前 `main` 分支与最近一个打了 `v*` 标签的发布版本。更早版本不提供安全修复。

## 报告漏洞

**首选渠道：GitHub 私有漏洞报告（Private vulnerability reporting）**

在仓库页面打开 **Security → Report a vulnerability** 提交报告。报告内容应包含：

- 影响范围（哪个脚本/哪个命令/哪种触发方式）
- 复现步骤与最小示例
- 影响说明（例如：是否可被 QQ 消息/群聊触发、是否涉及本机文件读写、是否泄漏数据）
- （如适用）建议的修复方式

**在私有漏洞报告未启用之前**：请先不要公开细节（不要在 issue、讨论、PR 或任何公开渠道贴出漏洞细节或 PoC），
可以先开一个**不包含细节**的 issue 说明「发现安全问题、已按 SECURITY.md 流程处理」，等待维护者开启私有报告通道或给出其他联系方式。

## 本项目已知的安全边界（报告前请先阅读）

- 本仓库**不接受任何凭据**：API Key、QQ openid、Market Token、AlecaFrame 解密密钥等一律不得写入代码、配置模板、文档或测试。
  发现测试/文档要求真实凭据即视为漏洞。
- 游戏与市场集成**只读**：任何「写操作」代码路径（挂单、改单、删单、游戏内自动化）都是违规实现。
- 个人数据（AlecaFrame 快照、QQ 身份）必须满足「主人私聊 + 精确身份匹配」双门。
- 安装/卸载脚本只处理**受管标记**的文件（`.warframe-assistant-managed.json` / 受控 AGENTS 片段 / 受管 WFInfo marker），
  卸载不得永久删除未标记内容；cron 操作只按精确 `declarationKey` 匹配。

## 安全更新流程

1. 修复提交到 `main`；
2. 若属已发布版本，按 [release.ps1](release.ps1) 流程发布新的 `vX.Y.Z` 补丁版本；
3. 在发布说明中描述影响与缓解措施（不暴露过多 PoC 细节）。

## 协调披露

本项目由个人维护者业余维护，**不承诺任何固定的披露时间窗口**（无 SLA，见 [SUPPORT.md](SUPPORT.md)）。
在此前提下，我们请求报告者配合以下协调方式：

1. 优先通过上面的私有渠道报告，并把「维护者确认收到报告」作为协调起点；
2. 在维护者确认收到并进入处理之前，请勿在公开渠道（issue、讨论、PR、群聊等）披露漏洞细节或 PoC；
3. 若报告长期未获任何回应（例如多次尝试仍无法联系到维护者），你可以自行决定公开，
   但请附上本文件链接，并说明维护者已知悉该问题。

维护者会尽力尽快确认与修复安全问题，但不对任何响应或修复时限作出承诺。
